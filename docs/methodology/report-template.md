# 项目洞察分析报告模板

## 0. 元数据

- 项目：
- URL：
- 分析日期：
- 分析方式：
- 采样范围：
- 最近提交：

## 1. 项目价值判断

| 项 | 结论 |
| --- | --- |
| 项目定位 |  |
| 核心用户 |  |
| 核心痛点 |  |
| 独特价值 |  |
| 关键指标 |  |
| 初步结论 |  |

证据摘要：

面向 HTML 展示时，本模块应拆成：

- 适合谁
- 解决什么问题
- 和别的方案哪里不同
- 为什么现在值得看
- 最小验证方式

## 2. Gold Example / Demo

- Demo 状态：
- Demo 载体相关性：若使用图片/视频，说明它直接展示了哪个核心产品界面、核心工作流、用户动作或输出结果；若不能说明，不使用该媒体。
- 典型输入：
- 预期流程：
- 预期输出：
- 价值证明：
- 未验证边界：

## 3. 核心业务流程与关键资产

### 项目机制图

- 图型选择：
- 选择理由：
- 场景：
- HTML 展示方式：交互式图表区，至少包含 `主流程` 与 `图源`；复杂项目可加入 `组件关系`、`系统动力学`。
- 移动端处理：当 SVG/图表缩小后不可读时，提供纵向步骤或回路 fallback。
- 图形要求：可见区域必须渲染为 SVG、Mermaid 图或等价可读图形，不把源码块当成最终图。
- Mermaid / 结构化图源：保留在 `图源` tab 或折叠区，便于审计和再编辑。

```mermaid

```

### 关键资产

| 资产 | 位置 | 作用 | 壁垒判断 |
| --- | --- | --- | --- |

## 4. 技术实现与可落地性

### 架构画像

- 项目复杂性评估结果：
- 选用的架构描述框架：
- 裁剪策略理由：
- 省略内容：

### 架构视角（可选）

先做复杂度评估，再决定使用 C4、4+1、UML 或轻量替代视角。简单/中等项目优先 C4 L1 + L2 + Dynamic；复杂/异构项目才使用 4+1 作为理论分类，并用 C4/UML/Mermaid 表达具体视图。核心业务流程交互图优先。

HTML 展示时，架构图必须按项目语义绘制或渲染：API/proxy 项目突出请求边界和适配链，桌面/大型应用突出工作流、数据总线和部署边界，规则/skill 项目突出行为约束和分发结构。Mermaid 或结构化图源应折叠保留，不能作为唯一可见表达。

#### 系统全貌

- 视图类型：
- 说明：

```mermaid

```

#### 核心业务流转 -> PRIORITY

- 视图类型：
- 场景描述：
- 交互细节：

```mermaid

```

#### 静态组织结构

- 视图类型：
- 说明：

```mermaid

```

#### 物理/部署视图（复杂项目可选）

```mermaid

```

### 运行机制

- 数据/请求流：
- 状态：
- 错误处理：
- 并发/异步：

### 工程化检查

| 维度 | 观察 | 判断 |
| --- | --- | --- |
| 安装/启动 |  |  |
| 依赖/配置 |  |  |
| API/CLI/SDK 契约 |  |  |
| 测试 |  |  |
| CI/CD |  |  |
| 日志/可观测性 |  |  |
| 文档 |  |  |

## 5. 风险、壁垒与最终建议

- 技术壁垒：
- 主要风险：
- 反证点：
- 最终建议：
- 下一步行动：

## 附录 A. 证据表

| 编号 | 类型 | 来源 | 支持的结论 |
| --- | --- | --- | --- |

## 附录 B. 展示层重排

如果面向首次接触项目的读者生成 HTML，可把五模块重排为：

1. 新用户先看什么
2. Gold Example / Demo
3. 项目机制图
4. 架构视角（可选；可使用 4+1）
5. 核心资产与价值
6. 采用前确认与证据边界

不要输出项目分数、总分或评分条。
