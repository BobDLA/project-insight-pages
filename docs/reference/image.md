根据以下元提示词 
系统动力学+UML混合模型生成规则
1. 全局设置 (专业、现代、融合风格)

一个现代、干净、专业的技术图示，类似于蓝图风格的信息图。
主标题与底部徽标：在图的顶部主标题区域和左下角底部清晰放置 [项目名] 动态行为流与系统增长逻辑，并使用项目特定标题（如 OpenClaw）。
颜色编码：
蓝色及其渐变：用于增强 (R) 增长回路和变量。
橙色（或灰色）及其渐变：用于平衡 (B) 控制回路和变量。
深灰色：用于骨架、生命线、文本。
中文和图标：所有关键标签、标题和描述都使用清晰、可见的中文。图标风格统一。
2. 顶部主标题与命令 (标准化)

在序列图顶部插入一个具体的示例任务，格式为：用户: [具体任务命令] 从 [渠道名/界面名]（例如：用户: 总结“file.txt”从 Telegram。）。
3. UML 序列图 (微观维度 - 中央骨架)

排列一系列垂直生命线，顶部有明确的项目角色图标和中文名称（如：用户、Telegram、Channel Manager、Gateway Daemon、LLM Adapter、Skill Engine, based on $MODULE_LIST）。
激活块和消息：在生命线上显示垂直激活块。使用带数字序号和中文描述的消息箭头（实线调用，虚线返回）。消息流应准确反映用户定义的 $SPECIFIC_TASK（如 OpenClaw 的 9 个步骤）。
4. 系统动力学 CLD (宏观维度 - 外部循环)

在外部布局 R 和 B 环路，强制使用标准化的逻辑模板。
自动化增强回路 (R)：
放置在序列图外部左侧或顶部，使用大的蓝色曲线箭头。
强制逻辑路径：[核心增长变量] -> (外部增强因素) -> [信任] -> [增强执行] -> [核心增长变量]。
标准化 R 符号：路径中央必须有一个明确的 +R 自动化增强增长回路 符号。箭头上标有 '+'。
系统控制平衡回路 (B) - 控制模板：
放置在序列图核心路径附近或右侧，使用橙色或灰色曲线箭头。
改进的关键逻辑模板： [用户未完成需求存量 (圆柱)] -> (触发) -> [序列图流程] -> (导致) -> [任务完成流量 (流)] -> (反馈反馈到反馈到需求降低流量] -> [用户需求降低反馈到初始存量]。
标准化 B 符号：路径中央必须有一个明确的 -B 系统控制与平衡 符号。箭头上标有 '-'。
5. 系统动力学：存量与流量 (宏观维度 - 路径集成)

改进的高级路径集成规则： 存量和流量组件（圆柱体）不再孤立，而是必须直接融入其所属的环路路径中。
存量 placement：
上下文存量： 位于序列图旁边。增加明确的 交互数据流量（例如，来自交互步骤 3 和 8）。添加解释文本块：“随着交互数据流入”。
技能/工具存量： 位于技能/执行路径上。增加 注册技能流量。添加注释：“随着新技能注册而增加”。
信任存量： 位于增长回路路径末端。增加明确的 成功任务流量（来自流程完成）。添加注释：“随着每个成功任务而增长”。
(New) 未完成需求存量： 位于控制回路的触发点，作为一个圆柱体。
流量： 必须使用明确的流量箭头（流）连接，描述流量性质（如：积累数据流量、技能注入流量）。
并行增长与输出： 将存量之间的连接概念化为“并行增长”。使用外部影响变量（如 数据清理）。在中心流程末端标准化 改进的 AI 性能 变量路径，并使用小型火花图标表示性能改进。
6. 最终细节与总结

检查所有中文清晰度、图标一致性。确保图像作为一个 cohesive 的视觉故事，清晰地展示“微观执行如何转化为宏观流量，驱动长期增长和需求满足”。

---- 生成
https://github.com/openclaw/openclaw
项目的 图片生成提示词

--然后生成图片